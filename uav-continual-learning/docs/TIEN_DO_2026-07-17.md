# Nhật ký tiến độ — 2026-07-17 (đợt triển khai cloud + sửa bug phần cứng thật)

> Nối tiếp `TIEN_DO_2026-07-14.md` (code G0–G3 + plan G4). Đợt này: hoàn thiện G4 wiring,
> bộ chạy-một-lệnh, triển khai GCP, và **7 bug thật** bị bắt/sửa khi chạy trên máy thật.

## 1. Trạng thái tổng thể (cập nhật)

| Hạng mục | Code | Số liệu | Ghi chú |
|---|---|---|---|
| G1 baseline (5 method) | ✅ | ✅ EuroSAT + RESISC45 | Cổng đạt từ 14/07 |
| G2 Titans A/B/C | ✅ | 🔄 **đang chạy trên GCP** | Bậc A (rimage) đã có kết quả; B/C đang chạy |
| M3 + Delta Momentum | ✅ (+2 fix mới) | 🔄 | 1D→Adam fallback, bias-corr V |
| G3 CMS | ✅ (+grad_agg) | ⬜ xếp hàng sau G2 | 4 ablation EuroSAT trong --quick |
| G4 HOPE | ✅ wiring xong | ⬜ chạy cuối --quick | HOPEClassifier + method hope + test |
| Hạ tầng 1-lệnh | ✅ | — | run_all.sh + make_report (docx) + resume |
| Triển khai | ✅ | — | GCP VM CPU e2-standard-8 (free trial), tmux |

## 2. Việc đã làm đợt này

- **G4 Phần A (S1–S5)**: `models/hope.py` (Titans-never trên backbone-CMS mở băng),
  memory+head vào tier nhanh nhất, method `hope` gộp log norm(state)+‖Δw‖, 2 config, 4 test.
- **Chuẩn paper hơn**: `cms.grad_agg: sum` (nguyên văn Eq. 71) | `mean` (biến thể, để ablate).
- **Bộ chạy một lệnh**: `run_all.sh` (G1→G4, resumable, tự chọn CMS winner cho G4, --quick/--shutdown)
  → `make_report.py` sinh `BAO_CAO_KET_QUA.docx` (bảng + biểu đồ + kết luận tự động; đã
  kiểm chứng bằng metrics giả lập).
- **Triển khai**: `docs/HUONG_DAN_GCP.md` (GPU L4 / CPU free-trial / SSH key từ Mac),
  `notebooks/colab_run_all.ipynb` (đường Colab miễn phí). Thực tế đã chọn: VM CPU
  e2-standard-8 (tài khoản free-tier không được cấp GPU).
- **Tài liệu**: `LOGIC_NESTED_LEARNING.md` (logic 4 tầng nhớ + 6 sai khác có chủ đích),
  `DANH_SACH_PAPER.md` (12 tài liệu đọc theo giai đoạn).

## 3. Bảy bug THẬT bị bắt khi chạy máy thật (nguyên liệu mục Lessons Learned)

| # | Bug | Gốc rễ | Sửa |
|---|---|---|---|
| 1 | `uavcl.data` biến mất trên VM | lệnh nén `--exclude='data'` khớp MỌI thư mục tên data → nuốt cả `src/uavcl/data` | exclude neo đường dẫn; scp bù |
| 2 | Double-backward ở G2 bậc B/C | state Titans chứa `tensordict.TensorDict` — hàm detach không nhận diện kiểu này → graph cũ dính lại | `state_utils` hiểu TensorDict + test "state graph-free" chẩn đoán đích danh |
| 3 | M3 dao động mạnh mấy bước đầu | thiếu bias-correction cho V kiểu Adam | thêm cho chế độ ema/delta |
| 4 | M3 "kẹt" trên tham số vector | áp NS+ký ức chậm lên 1D gây **limit-cycle** | quy ước Muon: ndim<2 rơi về Adam |
| 5 | Test hội tụ "trượt" với MỌI optimizer | đề test 20 phương trình/10 ẩn → loss tối ưu = 0.2358 ≠ 0; ba thuật toán khác nhau cùng ra 0.23582x = dấu hiệu ĐỀ sai, không phải thuật toán sai | b = A·x_true để nghiệm tồn tại |
| 6 | Crash `size 2 vs 3` giữa training | titans-pytorch giả định chuỗi chia hết chunk_size — batch lẻ cuối epoch (96 ảnh/chunk 64) làm lệch sổ chunk | wrapper pad-rồi-cắt; test quét đủ độ dài lẻ |
| 7 | Bậc B/C bị skip oan | tên run không chứa `memory.reset` → A/B/C trùng tên | run_name thêm `_rimage/_rtask/_rnever`; cột memory_reset vào metrics/báo cáo |

Bài học chung: **compile + logic test trong sandbox không thay được chạy thật** — 7/7 bug chỉ
lộ khi có torch/titans thật và dữ liệu thật. Vòng "pytest trước khi đốt giờ máy" đã hoàn vốn.

## 4. Đang chạy (thời điểm ghi)

`run_all.sh --quick --shutdown` trong tmux trên VM: G1 skip toàn bộ (mang số từ Mac),
G2 bậc A xong, bậc B (rtask) đang train ~1.85 it/s, sau đó C (rnever) + đối chứng AdamW
→ G3 4 ablation (phần nặng nhất trên CPU, vit_tiny 224px) → G4 HOPE → docx. Ước tính
tổng: đêm nay + một phần ngày mai; xong máy tự tắt.

## 5. Rủi ro còn mở

1. 🔴 G3/G4 trên CPU rất chậm (2–4h/run × 5) — nếu quá lâu: cắt bớt ablation
   (`--stages g4,report` sau khi có 2 cấu hình CMS) hoặc chờ nâng billing GPU.
2. 🟠 LwF bất thường trên EuroSAT (F 0.609) — chưa lý giải xong, thử λ nhỏ hơn khi rảnh máy.
3. 🟠 RESISC45 (bảng chính thức) chưa chạy — KHÔNG chạy trên CPU; chờ GPU hoặc Mac chạy đêm.
4. 🟡 Kết quả EuroSAT --quick chỉ là sân tập — kết luận cuối phải chờ RESISC45 + đa seed (G5).

## 6. Việc tiếp theo

1. Chờ VM xong → lấy `BAO_CAO_KET_QUA.docx` + `run.log` → **phiên đọc bảng G2/G3/G4**
   (never vs task vs image? đầu-chậm vs cuối-chậm? HOPE có 1+1>2?).
2. Đọc Titans (A1 trong `DANH_SACH_PAPER.md`) trong lúc chờ.
3. Sau khi có billing GPU / Mac rảnh đêm: chạy full RESISC45 (`run_all.sh` không --quick).
4. Đồng bộ code VM ↔ Mac (7 fix đã có ở cả hai — giữ thói quen scp sau mỗi lần sửa).
