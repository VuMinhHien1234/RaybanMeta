# Chẩn đoán + vá: `test_prequential_tang_nhanh_hoi_phuc...` đỏ (2026-08-04)

```
FAILED tests/test_ba_tang.py::test_prequential_tang_nhanh_hoi_phuc_con_dong_bang_thi_khong
AssertionError: U1 phải hồi phục (acc cuối 0.385)
1 failed, 227 passed
```

**Kết luận: lỗi ở TEST, không phải ở M1.** M1 chạy đúng. Test đang yêu cầu một việc bất khả thi.

---

## Nguyên nhân

`_du_lieu()` sinh tâm lớp `mu` từ **chính generator của `seed`**:

```python
def _du_lieu(n=300, seed=0, dich=None):
    g = torch.Generator().manual_seed(seed)
    mu = torch.randn(C, D, generator=g) * 3.0     # <-- mu phụ thuộc seed
    ys = torch.randint(0, C, (n,), generator=g)
    f = mu[ys] + torch.randn(n, D, generator=g) * 0.3
```

Test gọi hai lần với **hai seed khác nhau**:

```python
f,  ys = _du_lieu(400, seed=5)              # pha 1 — tâm lớp A
f2, y2 = _du_lieu(320, seed=6, dich=dich)   # pha 2 — tâm lớp B ≠ A, RỒI mới dịch
```

Nên pha 2 không phải "cùng khu vực, khác điều kiện" mà là **khu vực KHÁC HẲN**: SLDA học
tâm lớp A rồi bị chấm trên tâm lớp B. Tầng nhanh chỉ kéo lại phép **dịch toàn cục** — nó
không thể (và không được phép) sửa việc lớp bị thay ruột. Acc rơi về mức ngẫu nhiên.

`0.385 ≈ 1/C = 0.333` — đúng dấu hiệu đoán mò, không phải dấu hiệu căn chỉnh hỏng.

## Bằng chứng số

Tái hiện bằng numpy (`chan_doan_m1.py`, sandbox không có torch nên không trùng RNG — trùng
cơ chế), cùng một code M1, chỉ đổi một biến duy nhất là "pha 2 có dùng chung `mu` không":

| Cấu hình | U1 acc 3 batch cuối | U0 |
|---|---:|---:|
| `mu` KHÁC nhau giữa 2 pha (như test đang viết) | **0,30 – 0,44** | 0,25 – 0,41 |
| `mu` CHUNG (đúng bài toán) | **1,000** | 0,73 |

Đối chứng thứ ba: `mo_phong_ba_tang.py` — vốn dựng giao thức đúng, dùng chung một tập lớp —
cho U1 acc chéo 0,9977. Ba nguồn khớp nhau: cơ chế M1 lành.

Ablation phụ (loại trừ nghi ngờ ban đầu về số hạng co giãn `sqrt(v0/v_t)`):

| Ablation | U1 acc cuối |
|---|---:|
| `day_du` đầy đủ, decay 0,9 | 1,000 |
| bỏ hẳn co giãn, chỉ kéo mean | 1,000 |
| decay 0,99 (như config thật) | 1,000 (chậm hơn 2–3 batch) |

Méo tỷ lệ `\|log sqrt(v0/v_t)\|` max: **0,26 ở decay 0,9** · **0,06 ở decay 0,99** — nhỏ,
không phải thủ phạm. (Vẫn đáng ghi nhận: cửa sổ càng ngắn thì ước lượng phương sai theo
tỷ lệ lớp càng nhiễu — đây chính là bẫy T2, và là lý do `kieu: truc` tồn tại.)

---

## Bản vá (đã áp vào `tests/test_ba_tang.py`)

**1. Tâm lớp thành hằng số của cả file** — `seed` chỉ còn đổi mẫu + nhiễu:

```python
MU = torch.randn(C, D, generator=torch.Generator().manual_seed(0)) * 3.0

def _du_lieu(n=300, seed=0, dich=None):
    g = torch.Generator().manual_seed(seed)
    ys = torch.randint(0, C, (n,), generator=g)
    f = MU[ys] + torch.randn(n, D, generator=g) * 0.3
    ...
```

**2. Hướng dịch bám hình học lớp** thay vì hằng số:

```python
dich = 2.0 * (MU[1] - MU[0])      # thay torch.full((D,), 5.0)
```

Lý do: dịch theo hằng số `(5,5,…,5)` có thể tình cờ gần **song song với biên quyết định** —
lúc đó U0 không sập và assert thứ hai (`cuoi_u1 > cuoi_u0 + 0.2`) đỏ vì lý do khác. Trong
mô phỏng, 2/8 hình học lớp cho U0 vẫn đạt 0,95–1,00 với dịch hằng số. Dịch 2× vector nối
hai tâm thì đám mây lớp 0 chắc chắn rơi qua vùng lớp khác.

Kiểm bản vá trên 8 hình học lớp khác nhau:

| | U1 | U0 | hiệu |
|---|---:|---:|---:|
| 8/8 seed | 1,000 | 0,25 – 0,28 | ~0,75 |

Ngưỡng test là `U1 > 0,85` và `hiệu > 0,2` → biên an toàn rộng, không phải test may rủi.

## Việc cần làm

```bash
cd ~/Desktop/Raybanmeta/uav-continual-learning
pytest -q          # phải ra 228 passed
```

Xanh rồi thì tiếp tục PHẦN A2 của `CHAY_VM_BA_TANG_2026-08-04.md` (commit + push), nhớ
`git add` cả file test đã vá và file này.

## Ghi chú giữ lại cho lần sau

Bẫy "helper sinh dữ liệu tự đổi luôn hình học lớp theo seed" âm thầm biến mọi test
domain-shift thành test class-shift. Dấu hiệu nhận biết: **acc rơi đúng về 1/C**, và mọi
biện pháp căn chỉnh đều vô hiệu như nhau. Chú thích cảnh báo đã ghi ngay trên `MU`.
