# B2 — Ablation SLDA: hướng dẫn chạy

Ngày: 2026-08-02 · Nhánh đề xuất: `feat/slda-ablation`

## Câu hỏi thí nghiệm

> +11,5 điểm của SLDA so với NCM đến từ **hiệp phương sai chung** — đúng hay không?
> Và Σ có **cần cập nhật liên tục** không, hay ước lượng một lần là đủ?

## 4 arm, khác nhau đúng một biến

| Arm | Config | Bộ đọc | Kỳ vọng |
|---|---|---|---|
| 1 | `ncm_resisc45_frozen.yaml` | NCM thuần | 0.7114 |
| 2 | `slda_resisc45_streaming.yaml` | SLDA, Σ streaming | **0.8263** |
| 3 | `slda_resisc45_identity.yaml` | SLDA, **Σ = I** | ≈ arm 1 |
| 4 | `slda_resisc45_frozen.yaml` | SLDA, **Σ đóng băng sau task 0** | ? |

**Arm 3 là đối chứng sạch nhất.** Khi Σ = I:

```
argmax_c (μ_c·f − ½‖μ_c‖²)  ≡  argmin_c ‖f − μ_c‖²   =   NCM
```

Nó chạy qua **y hệt** đường code SLDA, nên chênh lệch arm 2 − arm 3 **chính là** đóng góp
của hiệp phương sai, không lẫn bất kỳ khác biệt cài đặt nào khác.

Đẳng thức này đã được kiểm bằng số: 0/300 dự đoán khác nhau (`tests/test_slda_cov_mode.py`).

Có cả arm 1 (NCM thuần, đường code riêng) để bắt lỗi cài đặt: **arm 1 và arm 3 phải cho
kết quả gần như trùng khít.** Lệch nhiều = có bug ở một trong hai.

## Cách đọc kết quả

| Quan sát | Kết luận |
|---|---|
| arm 3 ≈ arm 1 ≈ 0.71, arm 2 = 0.83 | ✅ **Hiệp phương sai là nguyên nhân.** Giả thuyết đúng |
| arm 3 ≈ arm 2 | ❌ Σ không quan trọng — cải thiện đến từ chỗ khác, phải tìm lại |
| arm 4 ≈ arm 2 | Σ **không cần** cập nhật liên tục → ước lượng 1 lần rồi đóng băng là đủ. **Tin tốt cho UAV**: bỏ được `gram` D×D khỏi bộ nhớ chạy |
| arm 4 < arm 2 rõ rệt | Σ **cần** streaming → chi phí O(D²) là bắt buộc, phải nêu trong phần chi phí |

Arm 4 có giá trị triển khai trực tiếp: nếu Σ đóng băng vẫn đủ tốt, drone chỉ cần mang
`μ_c` (135 KB) + `Λ` đã tính sẵn, **bỏ được ma trận `gram` 1,1 MB**.

---

# Lệnh chạy

## Bước 1 — pytest trên Mac (CHẶN)

```bash
cd ~/Desktop/Raybanmeta/uav-continual-learning && source .venv/bin/activate && pytest -q
```

Phải xanh cả `test_slda_cov_mode.py` (13 test mới) lẫn `test_gate_bound.py`.

## Bước 2 — Đo chi phí ngay trên Mac (B6, 2 phút)

```bash
python scripts/bench_cost.py --json bench_vits.json
python scripts/bench_cost.py --dim 1024 --json bench_vitl.json    # xem O(D²) phình ra sao
```

Không cần dataset, chạy được mọi máy. Cho ra bảng bộ nhớ / độ trễ / thời gian cập nhật
của SLDA (3 chế độ) và Titans.

## Bước 3 — Commit sang nhánh mới

```bash
cd ~/Desktop/Raybanmeta
rm -f .git/index.lock .git/HEAD.lock 2>/dev/null

git checkout -b feat/slda-ablation
git add -A
git commit -m "feat(slda): ablation cov_mode + do chi phi trien khai (B2, B6)

- slda.py: cov_mode = streaming | identity | frozen
  · identity: Sigma=I -> TUONG DUONG NCM (chung duong code) = doi chung sach nhat
  · frozen:   Sigma dong bang sau cov_freeze_after task, mu_c van cap nhat
- slda.py: memory_report() do byte THAT theo dtype (gram la float64)
- methods.py: SLDA.end_task goi on_task_end + in bo nho moi task
- run_g1.py: doc slda.cov_mode / slda.cov_freeze_after tu config
- 4 config arm: ncm_frozen + slda_{streaming,identity,frozen}
- scripts/bench_cost.py: do bo nho/do tre/thoi gian cap nhat, khong can dataset
- tests/test_slda_cov_mode.py: 13 test, gom kiem identity == NCM bang so

Mac dinh cov_mode=streaming -> ket qua 0.8263 bat bien."

git push -u origin feat/slda-ablation
```

## Bước 4 — Bật VM

Dùng lại `uavcl-cpu3` (đang TERMINATED, chưa ai dùng) — nó **đã có sẵn repo + venv +
dataset** từ campaign trước, nhanh hơn tạo máy mới rất nhiều.

```bash
gcloud compute instances start uavcl-cpu3 --zone=us-east1-b
```

Đợi 60–90 giây.

## Bước 5 — Chạy 4 arm × 3 seed

```bash
gcloud compute ssh uavcl-cpu3 --zone=us-east1-b
```

Trong VM:

```bash
cd ~/RaybanMeta && git fetch origin && git checkout feat/slda-ablation && git pull
cd uav-continual-learning && source .venv/bin/activate && pytest -q
```

pytest xanh thì:

```bash
tmux new -s slda
cd ~/RaybanMeta/uav-continual-learning && source .venv/bin/activate

nohup bash -c '
for ARM in ncm_resisc45_frozen slda_resisc45_streaming slda_resisc45_identity slda_resisc45_frozen; do
  for S in 0 1 2; do
    echo "=== $ARM seed $S  $(date +%H:%M)"
    .venv/bin/python scripts/run_g1.py \
      --config configs/$ARM.yaml \
      --set seed=$S train.eval_future=false log.dir=./artifacts_b2_${ARM}_s$S \
      > run_b2_${ARM}_s$S.log 2>&1
  done
done
echo "=== XONG TAT CA $(date +%H:%M)"
' > b2_campaign.log 2>&1 &

tail -f b2_campaign.log
```

Rời tmux: `Ctrl+B` rồi `D`.

**SLDA và NCM đều gradient-free** — engine bỏ hẳn vòng train, chỉ một lượt quét dữ liệu
mỗi task. Nên nhanh hơn Titans nhiều. Ước tính 20–40 phút/run → **4–8 tiếng cho cả 12 run**.

### Kiểm ngay sau khi arm đầu bắt đầu

Trong log phải thấy:

```
[slda] shrinkage=0.0001 cov_mode=identity
[slda] cov_mode=identity | bộ nhớ = ... MB (gram ... + means ... + cache ...)
```

Với arm 4 còn phải thấy dòng: `[slda] ĐÓNG BĂNG Σ sau task 0`.

Không thấy = checkout nhầm nhánh, dừng lại kiểm `git branch --show-current`.

## Bước 6 — Lấy kết quả về

```bash
gcloud compute ssh uavcl-cpu3 --zone=us-east1-b -- 'sudo bash -c "shopt -s nullglob; D=\$(ls -d /home/*/RaybanMeta/uav-continual-learning 2>/dev/null | head -1); cd \$D && tar czf /tmp/res_b2.tgz artifacts_b2_* run_b2_*.log b2_campaign.log && chmod 644 /tmp/res_b2.tgz && echo GOI_XONG \$D"'

mkdir -p ~/Desktop/Raybanmeta/result_test/b2
gcloud compute scp uavcl-cpu3:/tmp/res_b2.tgz ~/Desktop/Raybanmeta/result_test/b2/ --zone=us-east1-b
cd ~/Desktop/Raybanmeta/result_test/b2 && tar xzf res_b2.tgz

cd ~/Desktop/Raybanmeta
python3 uav-continual-learning/scripts/compare_all.py result_test/b2
```

## Bước 7 — Tắt VM

```bash
gcloud compute instances stop uavcl-cpu3 --zone=us-east1-b
```

---

## Bảng cần điền khi có kết quả

| Arm | Bộ đọc | Acc (3 seed) | Forget | AAA | Bộ nhớ |
|---|---|---|---|---|---|
| 1 | NCM thuần | | | | ~135 KB |
| 2 | SLDA Σ streaming | | | | ~1,32 MB |
| 3 | SLDA Σ = I | | | | ~135 KB |
| 4 | SLDA Σ đóng băng | | | | ~1,32 MB |

Đối chiếu với hai run Titans 9-task đang chạy trên `uavcl-cos` / `uavcl-sdc` là ra
**bảng so sánh hoàn chỉnh giữa hai hướng**, cùng split `combined31500_v2`, cùng 9 task ×
5 lớp — so trực tiếp được, không cần chú thích gì thêm.
