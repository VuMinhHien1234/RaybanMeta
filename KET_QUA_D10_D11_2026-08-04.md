# Kết quả D10 (λ) + D11 (Titans trên stream trôi)

Phân tích 2026-08-04 · D10: 15 run (5 arm × 3 seed) · D11: 6 run (2 trần η × 3 seed), 13h06

---

# PHẦN 0 — Cửa kiểm miễn phí: ĐẠT

Ở task 0–3 mức trôi mới 0–38%; hiệu chỉnh cho thấy vùng đó nằm trong nhiễu đo, tức **không có
gì để thích nghi**, nên 4 arm λ phải trùng nhau.

| task | arm1 | arm2 | arm3 | arm4 | spread |
|---|---|---|---|---|---|
| 0 | 0,7756 | 0,7770 | 0,7793 | 0,7784 | 0,0037 |
| 1 | 0,8107 | 0,8111 | 0,8125 | 0,7989 | 0,0136 |
| 2 | 0,8173 | 0,8173 | 0,8121 | 0,8051 | 0,0123 |
| 3 | 0,8218 | 0,8223 | 0,8232 | 0,8086 | 0,0147 |

Spread ≤ 0,015, đúng cỡ nhiễu. **λ cài đúng.** Đọc tiếp được.

---

# PHẦN 1 — D10: λ CÓ tác dụng, nhưng thước đo chuẩn che mất

## Nhìn theo cách chuẩn của continual learning

| arm | λ_μ | λ_Σ | Acc | std | AAA |
|---|---:|---:|---:|---:|---:|
| 1 | 1,0 | 1,0 | **0,7817** | 0,0049 | **0,8033** |
| 2 | 0,999 | 0,9999 | 0,7815 | 0,0030 | 0,8030 |
| 3 | 0,99 | 0,9999 | 0,7692 | 0,0039 | 0,7984 |
| 4 | 0,97 | 0,9999 | 0,7033 | 0,0023 | 0,7712 |
| 5 | 0,99 | **0,99** | 0,5029 | 0,0180 | 0,6175 |

Đọc thẳng bảng: **λ=1 thắng, càng quên càng tệ, không có hình chữ U.** Giả thuyết bị bác bỏ.

Nhưng đó là kết luận sai, vì thước đo sai.

## Vì sao thước đo chuẩn không hợp

`AAA` và `Acc` trung bình trên **mọi task đã thấy**. Sau task 8, mô hình bị chấm trên 9 bộ test
— **8 bộ là điều kiện QUÁ KHỨ**, chỉ 1 bộ là hiện tại. Thước đo đó thưởng cho việc nhớ lịch sử.

Drone thì bay **bây giờ**, dưới điều kiện **bây giờ**. Không ai trả tiền để nó nhận diện tốt
ánh sáng của ba tháng trước.

## Đo theo điều kiện hiện tại thì đảo chiều

| arm | λ_μ | TB mọi task | Điều kiện hiện tại (đường chéo) |
|---|---:|---:|---:|
| 1 | 1,0 | **0,8033** ± 0,0030 | 0,7666 ± 0,0043 |
| 2 | 0,999 | 0,8030 ± 0,0024 | 0,7673 ± 0,0036 |
| 3 | **0,99** | 0,7984 ± 0,0026 | **0,7759** ± 0,0045 |
| 4 | 0,97 | 0,7712 ± 0,0022 | 0,7671 ± 0,0053 |
| 5 | 0,99* | 0,6175 ± 0,0029 | 0,5954 ± 0,0014 |

**Hình chữ U ngược xuất hiện**, đỉnh ở arm 3 — đúng như dự đoán, chỉ là ở thước đo kia.

arm3 so arm1, 3 seed:

| Cách đo | Chênh | σ |
|---|---:|---:|
| TB mọi task | −0,0049 | −2,16 |
| Đường chéo (hiện tại) | **+0,0093** | **+2,59** |
| Test t8 (trôi 100%) | **+0,0391** | **+3,10** |
| Test t0 (điều kiện cũ) | −0,0454 | −3,53 |

## Bằng chứng đẹp nhất: lợi ích của λ tăng ĐƠN ĐIỆU theo mức trôi

arm3 − arm1, trên từng bộ test sau khi học hết:

```
trôi   0%  −0,0454   ████████▌          λ=1 thắng
trôi  12%  −0,0458   ████████▌
trôi  25%  −0,0344   ██████▌
trôi  38%  −0,0316   ██████
trôi  50%  −0,0124   ██▌
trôi  62%  −0,0082   █▌
trôi  75%  +0,0005   ·                  ← ĐIỂM HOÀ
trôi  88%  +0,0253          ████▌
trôi 100%  +0,0391          ███████     λ=0,99 thắng
```

Đánh đổi **thiên lệch–phương sai** hiện ra bằng số, đơn điệu, cắt trục ở ~75% trôi. λ<1 đổi
*độ chính xác trên điều kiện cũ* lấy *độ bám điều kiện mới*; điều kiện càng lệch xa quá khứ
thì đổi càng lãi.

## arm5 xác nhận: PHẢI tách hai λ

λ_Σ = 0,99 → Σ chỉ còn **100 mẫu hiệu dụng** cho ma trận **384×384** → suy biến, mất 18,6 điểm.
Đối chứng âm chạy đúng thiết kế, và chứng minh **dùng một λ chung cho cả μ và Σ là sai về
nguyên tắc**, không phải chuyện tinh chỉnh.

---

# PHẦN 2 — D11: hai phát hiện sạch, một phép so KHÔNG hợp lệ

## 2.1 ⭐ η KHÔNG phải là một cần gạt

Đổi trần η **gấp 100 lần** — và accuracy gần như không nhúc nhích:

| | Acc | AAA | Đường chéo |
|---|---:|---:|---:|
| `gates` η ∈ [0,100 · 0,900] | 0,49369 ± 0,0077 | 0,3090 | 0,2759 |
| `eta_thap` η ∈ [0,012 · 0,500] | 0,49370 ± 0,0074 | 0,3094 | 0,2765 |
| **chênh** | **+0,0000157** | +0,0004 | +0,0006 |

Từng seed: `0,484982 / 0,496408 / 0,499666` so với `0,486284 / 0,493735 / 0,501084` — lệch nhau
±0,002, tức nhiễu seed thuần tuý. Trần **thật sự** khác nhau (log xác nhận `η∈[1.000e-01,
9.000e-01]` so với `η∈[1.213e-02,5.000e-01], tâm −2.2`), và η đo được **thật sự** khác:

```
gates     η: 0,476 → 0,554 → 0,634 → 0,699 → 0,717 → 0,737 → 0,768 → 0,771 → 0,779
eta_thap  η: 0,362 → 0,394 → 0,425 → 0,443 → 0,451 → 0,462 → 0,473 → 0,476 → 0,482
```

Mà α thì **gần như trùng khít** giữa hai cấu hình:

```
gates     α: 0,627 0,700 0,769 0,842 0,789 0,603 0,291 0,255 0,248
eta_thap  α: 0,629 0,707 0,778 0,846 0,812 0,616 0,316 0,294 0,252
```

**Giả thuyết cơ chế:** `titans_head.py:128` có `self.post_norm = nn.LayerNorm(dim)` áp **ngay
sau** memory. η điều khiển *độ lớn* bước ghi vào bộ nhớ; LayerNorm chuẩn hoá lại thang đo
trước khi head nhìn thấy. Nói cách khác, **lớp chuẩn hoá đang triệt tiêu chính cơ chế mà ta
đang cố nghiên cứu.** Chưa kiểm chứng, nhưng nó khớp hoàn hảo với dữ liệu và kiểm được bằng một
ablation 1 biến.

Đây là phát hiện có giá trị: 3 seed, 1 biến, kết quả rõ ràng là **không**. Nó cũng có nghĩa
toàn bộ công vá trần η hôm qua là *đúng về kỹ thuật nhưng vô ích về hiệu quả* — và bây giờ ta
biết điều đó bằng số thay vì phỏng đoán.

## 2.2 Cổng bão hoà NHẸ hơn hẳn ở regime trôi

| | η chạm trần | α chạm sàn |
|---|---:|---:|
| gates s0 / s1 / s2 | 0 / 0 / 0 | 0 / 3 / 2 (trên 9 task) |
| eta_thap s0 / s1 / s2 | 4 / 0 / 0 | 0 / 3 / 2 |

Khác hẳn class-incremental, nơi α **kẹt cứng ở 0,9993 từ task 5**. Ở đây α đi
`0,63 → 0,85 (t3) → 0,25 (t8)` — **không đơn điệu**, lên rồi xuống, tức vẫn đang phản ứng với
dữ liệu. Regime trôi thân thiện với cổng hơn regime lớp-mới.

Nghĩa là kết luận hôm qua *"gate_bound đã cạn"* **quá bi quan**. Bão hoà chỉ xảy ra 0–4/9 task
tuỳ seed, không phải kẹt biên toàn cục.

## 2.3 ⚠️ Phép so Titans với SLDA KHÔNG hợp lệ ở lần chạy này

| | AAA | Đường chéo |
|---|---:|---:|
| Titans | 0,3090 | 0,2759 |
| SLDA arm3 | 0,7984 | 0,7759 |

Nhìn thì Titans thua 50 điểm. **Đừng kết luận vậy.** Quỹ đạo accuracy trên chính task đang học:

```
Titans:  0,051  0,079  0,168  0,212  0,298  0,363  0,386  0,393  0,370
SLDA  :  0,775  0,818  0,829  0,823  0,802  0,822  0,744  0,702  0,630
```

Titans khởi đầu **0,051** — trong khi đoán mò 45 lớp là 0,022. Nó đang học **từ số không**.

Nguyên nhân: config D11 **quên bật `train.eval_ncm_head`**, nên bộ đọc duy nhất là một
linear head huấn luyện từ đầu bằng 3 epoch trên 1/9 dữ liệu. SLDA thì là công thức đóng, mạnh
ngay từ task 0.

Vậy phần lớn khoảng cách 50 điểm là **bộ đọc**, không phải **cơ chế nhớ**. Lần chạy này không
trả lời được câu hỏi ta muốn hỏi. Đây là lỗi thiết kế của tôi ở khâu dựng config — cùng loại
với lỗi gộp ba thay đổi vào một arm hồi tuần trước.

`run_g1.py:264` có sẵn cơ chế: bật `train.eval_ncm_head` là nó sinh `metrics_ncm.json` với
prototype dựng trên feature **sau memory**, không train lại. Đó mới là phép so giữ nguyên bộ đọc.

---

# PHẦN 3 — Nên làm gì tiếp

## Ưu tiên 1 — SLDA HAI TẦNG (~3h code + 1 đêm)

Dữ liệu chỉ thẳng vào đây. Không arm nào **thống trị**: arm1 hơn 4,5 điểm ở điều kiện cũ,
arm3 hơn 3,9 điểm ở điều kiện mới. Cả hai đang bỏ tiền trên bàn. Giữ **cả hai**:

```
μ_chậm   λ = 1        toàn bộ lịch sử, ít nhiễu, mạnh ở điều kiện cũ
μ_nhanh  λ = 0,99     cửa sổ ~100 mẫu, bám trôi, mạnh ở điều kiện mới
Σ        λ = 0,9999   dùng chung (arm5 đã chứng minh Σ cần cửa sổ dài)
```

Đây **chính là Continuum Memory System** của bài Nested Learning — bộ nhớ nhiều tầng khác tần
số — nhưng bằng công thức đóng:

| | Titans / NL | SLDA hai tầng |
|---|---|---|
| Nhiều tầng tần số | ✅ | ✅ |
| Cần gradient | có | **không** |
| Cổng trôi ra biên | có (đã đo) | **không thể** — λ cố định |
| Bộ nhớ thêm | 102,68 MB | **+0,14 MB** |

Cách trộn cần một thí nghiệm nhỏ: trọng số cố định `w ∈ {0,25 · 0,5 · 0,75}`, hoặc chọn theo
khoảng cách Mahalanobis tới từng tầng.

Nếu ăn thì đây là kết quả mạnh nhất của dự án: *ý tưởng bộ nhớ nhiều tầng của Nested Learning
đúng — nhưng không cần học nó bằng gradient.*

## Ưu tiên 2 — Ablation `post_norm` (~4h máy, 0 code)

Kiểm giả thuyết ở 2.1: `post_norm` có đang triệt tiêu η không? Chạy `eta_thap` với
`post_norm: false`, 3 seed. Nếu lúc đó η **có** ảnh hưởng tới accuracy thì ta đã tìm ra
nguyên nhân thật, và mọi kết luận cũ về cổng phải đọc lại.

Rẻ, một biến, và có thể lật lại toàn bộ mạch Titans.

## Ưu tiên 3 — Chạy lại D11 với `train.eval_ncm_head: true` (~13h, 0 code)

Chỉ làm nếu muốn tuyên bố điều gì đó về Titans. Hiện tại **không được phép** nói "Titans thua
SLDA 50 điểm" — số đó lẫn bộ đọc.

## Sửa ngay trong cách viết báo cáo

Luôn báo cáo **hai** con số: TB mọi task (để so với văn liệu) và điều kiện hiện tại (vì đó là
thứ có nghĩa cho UAV). Đưa một con số là bỏ mất kết quả chính — đúng cái suýt xảy ra sáng nay.

---

# Những gì ĐƯỢC phép tuyên bố hôm nay

1. λ cài đúng (cửa kiểm ĐẠT), và **λ = 0,99 hơn λ = 1 trên điều kiện hiện tại**: +0,93 điểm
   đường chéo (2,59σ), +3,91 điểm ở mức trôi 100% (3,10σ).
2. Lợi ích của λ **tăng đơn điệu theo mức trôi**, cắt trục ở ~75%. Đánh đổi thiên lệch–phương
   sai, đo được.
3. **Phải tách hai λ**: λ_Σ = 0,99 làm Σ suy biến (100 mẫu < 384 chiều), mất 18,6 điểm.
4. **η không phải cần gạt**: đổi trần 100× → Δacc = 0,00002. Nghi `post_norm` triệt tiêu.
5. Ở regime trôi, cổng bão hoà **nhẹ hơn hẳn** class-incremental (0–4/9 task, α không đơn điệu).

# Những gì CHƯA được phép tuyên bố

- Bất cứ điều gì so sánh Titans với SLDA — lần chạy này lẫn biến bộ đọc.
- Rằng `post_norm` là thủ phạm — mới là giả thuyết khớp dữ liệu, chưa có ablation.
