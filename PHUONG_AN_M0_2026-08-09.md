# Phương án đề xuất: **M0 — Tầng nền hiệu chỉnh, meta-học ở pha 1**

Lập 2026-08-09, sau khi đối chiếu `BAI_TOAN_VA_MUC_TIEU_2026-08-04.md` với `NL.pdf` và toàn bộ kết quả đã đo.

---

# PHẦN I — Đọc lại mục tiêu: đã đạt gì, còn thiếu gì

## I.1. Bảng đối chiếu O1–O4

| | Mục tiêu | Trạng thái | Số |
|---|---|---|---|
| **O1** | Giữ danh tính lớp | ✅ đạt | SLDA 0.8262 (class-inc) · 0.628 (revisit) |
| **O2** | Bám trôi không nhãn | ✅ **đạt, có ý nghĩa thống kê** | U1−U0 = **+0.0319 ± 0.0109**, t=5.07, p≈0.037 |
| **O3** | Ghi nhớ điều kiện | ❌ **đã trả lời: KHÔNG** | U2−U1 = −0.0006 ± 0.0010 (cả chuyến); ứng viên B còn **hại** −0.0121 |
| **O4** | Chạy trên drone | ✅ đạt rất thoải mái | 0.031 MB / 10 MB · 0.069 ms / 1.67 ms |

O3 **không phải câu hỏi mở nữa**. Nó có câu trả lời, có cơ chế định lượng: λ=0,99 · batch 32 → `r = 0.725/batch` → tầng nhanh hội tụ trong ~4 batch. Snapshot lưu trữ (trung bình EMA qua nhiều lần gặp) **mờ hơn** ước lượng tươi 5 batch. Nhớ lại tệ hơn ước lượng lại.

## I.2. ⭐ Khoảng trống LỚN NHẤT chưa ai chạm vào

Từ `cua_chan.log` — hiệu chỉnh trôi trên feature đóng băng:

```
NCM ở  0% trôi   0.6900
NCM ở 100% trôi  0.5310
                 ───────
trôi lấy mất     15,9 điểm
tầng nhanh cứu   +3,2 điểm  =  20%
CÒN LẠI          12,7 điểm  =  80%   ← CHƯA AI CHẠM
```

**12,7 điểm còn trên bàn — gấp 4 lần tất cả những gì đã giành được.**

Vì sao tầng nhanh không lấy được? Vì nó chỉ biểu diễn được **biến đổi affine cố định**:

```python
# tang_nhanh.py::can_chinh — kiểu day_du
ty_le = sqrt(v0 / v_t)
return (f − m_t) * ty_le + m0
```

Dịch + co giãn từng chiều. Hệ số `(γ, β)` tính bằng **công thức đóng**, không học.

Và T1 đã báo trước: `ket_luan_t1 = "MOT_PHAN"`, `cos_trung_binh = 0.7497`. Điều kiện **không** nằm gọn trên một trục — tác động của nó lên không gian feature có phần **phi tuyến**.

Chính file mục tiêu §7 đã ghi rủi ro này:

> *"bản đóng chỉ biểu diễn được biến đổi **affine**. Nếu điều kiện tác động **phi tuyến** lên không gian feature thì phải quay lại Titans."*

Số đo đã xác nhận vế trước. Nhưng kết luận vế sau — *"phải quay lại Titans"* — **sai**, vì Titans vi phạm O4 gấp 10,3 lần và thua cả NCM trần.

**Cần một cách khác để vượt affine mà vẫn giữ O4.** Đó là toàn bộ nội dung phương án này.

---

# PHẦN II — Đọc lại paper: cơ chế nào CHƯA được cài?

## II.1. Lõi của Nested Learning

Từ abstract:
> *"a set of nested, multi-level, and/or parallel optimization problems, each with its own context flow"*

Từ §3:
> *"multiple levels to learn from and process data with different levels of abstraction and **frequencies of update**"*

Ba đóng góp: **(1)** optimizer biểu cảm (M3) · **(2)** module tự-sửa (Titans, Eq 76–82) · **(3)** **CMS** (Eq 70–75).

## II.2. CMS — Eq 70/71

```
y_t = MLP^(f_k)( … MLP^(f_1)(x_t) )                                    (70)

θ^(f_ℓ) cập nhật MỖI C^(ℓ) bước, bằng gradient GỘP của cửa sổ đó       (71)
```

Một **chuỗi khối**, mỗi khối một tần số. Đây là đóng góp **khớp nhất** với cấu trúc ba tần số của bài toán UAV.

## II.3. ⭐ Eq 72/73 + §7.3 — cơ chế mà **KHÔNG nhánh nào của dự án đã cài**

Đây là chỗ tôi muốn dừng lại lâu.

```
θ₀^(f_{s+1}) = arg min_Φ  E_{T∼C^(s)} [ ℓ(Θ, T; Φ) ]                   (72)
```

> *"the initial state of the MLP block in level s+1 is **meta-learned in level s**"*

Và §7.3 (*Ad-hoc Level Stacking*) nói rõ mục đích:

> *"**higher frequency level can take advantage of the knowledge stored in the lower frequency level and so adapt faster to its own context flow.**"*

Và §8.1, về trạng thái khởi tạo của mọi memory:

> *"the meta-learning of the initial states of memories is **essential** for both fast-adaption, training stability, robustness to noise"*

**Đối chiếu với dự án:**

| Cơ chế NL | Nhánh Titans/HOPE | Nhánh ba tầng |
|---|---|---|
| (1) Expressive optimizer (M3) | ✅ cài | ❌ |
| (2) Self-referential (Eq 76–82) | ✅ cài | ❌ |
| (3) CMS đa tần số (Eq 70–71) | ✅ cài, ❌ chưa từng chạy (3 lần) | ✅ cài bằng công thức đóng |
| **Eq 72/73 — meta-học trạng thái khởi tạo tầng** | ❌ **không** | ❌ **không** |

Hiện `chot_moc()` chỉ lấy **trung bình thô** của pha 1:
```python
def chot_moc(self):
    self.m0, self.v0 = self.m_t.clone(), self.v_t.clone()
```

Đó là *ghi nhận* một mốc, không phải *meta-học* một trạng thái khởi tạo tối ưu. Paper nói mốc này phải là nghiệm của một bài toán tối ưu ở tầng tần số thấp hơn.

> **Eq 72/73 là cơ chế NL duy nhất chưa từng được cài, và nó nhắm đúng vào khoảng trống 12,7 điểm.**

---

# PHẦN III — Phương án M0

## III.1. Ý tưởng một câu

> Thay công thức đóng `γ = √(v0/v_t)`, `β = m0 − m_t` bằng **hai hàm học được** `γ_φ(s)`, `β_φ(s)` — meta-học ở pha 1 trên dữ liệu **trôi tổng hợp**, với `s` là mô tả điều kiện **ước lượng được không cần nhãn**.

## III.2. Kiến trúc bốn tầng

```
tầng NỀN   (M0)  φ = (γ_φ, β_φ)      meta-học 1 lần ở pha 1, CÓ nhãn      ← Eq 72/73   [MỚI]
tầng chậm        μ_c, Σ  (SLDA)      học 1 lần ở pha 1, CÓ nhãn
tầng NHANH (M1)  m_t, v_t            mỗi batch, KHÔNG nhãn                 ← đã có, +3,2đ
tầng trung (M2)  ngân hàng chế độ    1 lần/chuyến                          ← đã đo: ≈ 0, BỎ
```

Luồng ở pha 2 (không nhãn):

```
ảnh → ViT đóng băng → f
                      ↓
       tầng nhanh cập nhật (m_t, v_t)          [không nhãn]
                      ↓
       s_t = [m_t − m0 ; log(v_t/v0)]          mô tả điều kiện, 768 chiều
                      ↓
       f' = γ_φ(s_t) ⊙ f + β_φ(s_t)           ← M0: PHI TUYẾN theo điều kiện
                      ↓
       SLDA → logits
```

**Điểm mấu chốt:** `f' = γ ⊙ f + β` vẫn **affine theo f** (rẻ), nhưng `γ, β` là hàm **phi tuyến của điều kiện `s`**. Bản hiện tại ép `γ = √(v0/v_t)`, `β = m0 − m_t` — một dạng hàm **cố định**. M0 cho phép học dạng hàm đó.

Đây là FiLM-style conditioning, và nó **chính là** Eq 72/73: tham số của tầng tần-số-cao được meta-học ở tầng tần-số-thấp.

## III.3. Huấn luyện M0 ở pha 1

Pha 1 theo phát biểu bài toán §2: *"Không giới hạn thời gian và tính toán. Chạy một lần."*

```
1. Trích f₀ = backbone(x) cho toàn bộ D₀ (điều kiện e₀, CÓ nhãn)
2. Với mỗi mức trôi m ∈ {0.25, 0.5, 0.75, 1.0} (drift.py đã có sẵn):
       f_m   = backbone(ApDungTroi(m)(x))
       s_m   = [mean(f_m) − m0 ; log(var(f_m)/v0)]     ← thống kê, KHÔNG nhãn
3. Huấn luyện φ tối thiểu hoá:
       L = ‖ γ_φ(s_m) ⊙ f_m + β_φ(s_m) − f₀ ‖²   +   λ_ce · CE(SLDA(f'), y)
       └──────────── tái dựng ────────────┘       └──── giữ khả năng phân lớp ────┘
```

Kích thước: `s` 768 chiều → MLP `768 → 256 → 768×2`. **≈ 0,6M tham số ≈ 2,4 MB fp32.**

## III.4. Đối chiếu O4 — vẫn đạt

| | Bộ nhớ | Độ trễ/khung |
|---|---:|---:|
| SLDA (tầng chậm) | 1,50 MB | 0,0071 ms |
| tầng nhanh (M1) | 0,006 MB | 0,047 ms |
| **M0 (mới)** | **2,4 MB** | **~0,05 ms** (2 matmul 384×256) |
| **TỔNG** | **3,9 MB** | **~0,10 ms** |
| Trần | **10 MB** | **1,67 ms** |

✅ Còn dư **2,6×** bộ nhớ và **17×** độ trễ. So với Titans: 102,68 MB / 2,178 ms.

---

# PHẦN IV — Vì sao phương án này phù hợp nhất

## IV.1. So với mục tiêu bài toán

| | |
|---|---|
| **Nhắm đúng khoảng trống lớn nhất** | 12,7 điểm chưa ai chạm, gấp 4 lần thành quả hiện tại |
| **Không phá gì đã có** | M0 nằm *giữa* backbone và SLDA. U0/U1 vẫn là mốc so hợp lệ |
| **Giữ đúng luật nhãn** | `s` tính từ thống kê feature, **không dùng nhãn** ở pha 2. `count_raw` vẫn đứng yên — test cũ vẫn bắt được |
| **Giữ O4** | 3,9 MB < 10 MB · 0,10 ms < 1,67 ms |
| **Đúng cấu trúc ba tần số** | thêm một tầng *tần số thấp nhất*, không phá ba tầng đang có |

## IV.2. So với paper — đây là phần mạnh nhất

| Tiêu chí | M0 |
|---|---|
| **Eq 72/73** — meta-học trạng thái khởi tạo tầng | ✅ **Cài lần đầu trong dự án.** `chot_moc()` hiện chỉ lấy trung bình thô |
| **§7.3** — *"higher frequency level takes advantage of knowledge stored in lower frequency level to adapt faster"* | ✅ đúng nguyên văn: tầng nhanh (mỗi batch) dùng kiến thức tầng nền (1 lần) |
| **Eq 70/71 — CMS** | ✅ chuỗi 4 khối, 4 tần số: 1 lần → 1 lần → mỗi batch → (bỏ M2) |
| **§3 — mỗi tầng một context flow riêng** | ✅ M0 học trên *phân bố điều kiện*; M1 học trên *dòng hiện tại*; SLDA học trên *danh tính lớp* |
| **Eq 9/21/33** — cập nhật dạng đóng vẫn là level NL hợp lệ | ✅ giữ nguyên lập luận đã có |

**Với M0, dự án cài được cả 3 đóng góp của NL bằng công thức rẻ, cộng thêm cơ chế Eq 72/73 mà nhánh gradient cũng chưa cài.**

---

# PHẦN V — ⛔ Cửa chặn 1 giờ TRƯỚC khi viết dòng code nào

Đây là bước bắt buộc, theo đúng văn hoá *"kiểm setup 15 phút, tránh mất 8 tiếng"* của dự án.

**Câu hỏi:** trong 12,7 điểm còn lại, **bao nhiêu là lấy lại được?** Mờ Gauss σ=1,4px **xoá thông tin thật** — không phép hiệu chỉnh nào lấy lại được.

**Thí nghiệm trần trên (oracle), ~1 giờ, không cần GPU:**

```
1. Trích f₀ (sạch) và f₁ (trôi 100%) trên 2000 mẫu test
2. Fit hồi quy tuyến tính ORACLE W: f₁ → f₀   (dùng cặp khớp — KHÔNG triển khai được,
   chỉ để đo trần)
3. Chấm NCM/SLDA trên W·f₁
```

| Kết quả | Kết luận |
|---|---|
| ≥ 0,65 (lấy lại ≥ 75%) | ✅ **Còn nhiều để giành. Làm M0.** |
| 0,58–0,65 | 🟡 Trần ~50%. Vẫn đáng, nhưng hạ kỳ vọng |
| ≤ 0,58 (lấy lại < 30%) | ❌ **Thông tin đã mất thật. BỎ M0**, chuyển sang giảm severity hoặc thích nghi backbone |

**Không chạy cửa chặn này thì đừng bắt đầu.** Nó đúng loại sai lầm mà D10 (AAA che λ) và U2 (`cho_khop_sau` ngoài cửa sổ) đã mắc: đầu tư trước khi biết có gì để đo.

---

# PHẦN VI — Rủi ro, và cách khử từng cái

| # | Rủi ro | Mức | Cách khử |
|---|---|---|---|
| **R1** | Trần thật thấp (mờ xoá thông tin) | 🔴 cao | **Cửa chặn Phần V** — 1 giờ, quyết định đi/dừng |
| **R2** | Overfit vào họ trôi tổng hợp; điều kiện thật khác | 🟡 vừa | **Giao thức giữ lại:** huấn luyện trên mức `{0.25, 0.75}`, chấm trên `{0.5, 1.0}`. Và: huấn luyện trên 4/5 phép biến đổi, chấm trên đủ 5 |
| **R3** | "Dùng trước bộ sinh trôi = gian lận" | 🟡 vừa | Nêu rõ là **giả định mô hình**: nhà sản xuất drone **biết** đáp ứng quang học của camera. Đây là domain randomization chuẩn. Và R2 kiểm được tính tổng quát |
| **R4** | Định vị: thành "domain generalization", bớt tính continual | 🟡 vừa | Phần trực tuyến **vẫn không nhãn, vẫn liên tục**. M0 là *tầng tần số thấp nhất* của CMS — đúng Eq 72/73, không phải tiền xử lý rời |
| **R5** | Thêm 2,4 MB | 🟢 thấp | 3,9/10 MB. Nếu chật: giảm hidden 256→128 (0,6 MB) |
| **R6** | Cần code mới (~200 dòng + pha 1 offline) | 🟢 thấp | Tái dùng `drift.py`, `tang_nhanh.py`, `slda.py`, stream revisit, toàn bộ metrics. Chỉ thêm 1 module + 1 script pha 1 |
| **R7** | Không trả lời O3 | 🟢 thấp | O3 **đã có câu trả lời** (không). M0 nhắm O1/O2 — nơi còn 12,7 điểm |

---

# PHẦN VII — So với các hướng đã làm và đang cân nhắc

| Hướng | Nhắm gì | Trần | O4 | NL-đúng | Tình trạng |
|---|---|---|---|---|---|
| **Titans / HOPE** | thay bộ nhớ | oracle 0.6874 < NCM trần 0.6933 | ❌ **102,68 MB** | ✅ (2)(3) | Cổng sống, state tích luỹ, **vẫn thua**. Chỉ giữ làm đối chứng |
| **M3 optimizer** | tối ưu hoá | thua AdamW (0.424 vs 0.577) | — | ✅ (1) | Nhánh chính **không dùng optimizer**. Dừng |
| **M2 ngân hàng** | O3 | −0.0006 ± 0.0010; ứng viên B **hại** −0.0121 | ✅ | ⚠ một phần | **Đã trả lời: không.** Kết quả âm có cơ chế |
| **λ-sweep (0.999)** | ranh giới O3 | tối đa ~0,01 điểm; đánh đổi O2 | ✅ | ⚠ | Bổ trợ, không phải hướng chính |
| **⭐ M0 tầng nền** | **12,7 điểm** | **12,7 điểm** (chờ cửa chặn) | ✅ **3,9 MB** | ✅ **(3) + Eq 72/73** | **Đề xuất** |

## VII.1. Điểm mạnh của M0

1. **Trần cao gấp 4 lần** mọi hướng còn lại (12,7 vs ~3,2 đã giành, vs ~0,01 của λ-sweep).
2. **Cài cơ chế NL mà cả hai nhánh đều bỏ trống** — Eq 72/73. Nói được: *"chúng tôi cài đóng góp thứ 3 của NL đầy đủ hơn cả bản gradient."*
3. **Giữ O4 với biên rộng** — lập luận chi phí (thứ duy nhất không bác được) vẫn nguyên vẹn.
4. **Tấn công đúng nguyên nhân đã đo được**, không phải đoán: T1 = `MOT_PHAN` nói điều kiện phi tuyến; affine chỉ lấy 20%.
5. **Không phá mốc cũ.** U0/U1/U2 vẫn so được. Mọi cửa chặn, test, thước đo dùng lại.
6. **Có cửa chặn rẻ (1 giờ)** giết được nó trước khi tốn giờ máy.

## VII.2. Điểm yếu — nói thẳng

1. **Dùng trước tri thức về họ trôi.** Ba tầng hiện tại thì không. Đây là **giảm tính tổng quát**, phải nêu rõ và kiểm bằng giao thức giữ-lại (R2).
2. **Không trả lời câu hỏi ban đầu.** Câu hỏi trung tâm ban đầu là O3 "ghi nhớ điều kiện". M0 **đổi câu hỏi** sang "hiệu chỉnh điều kiện tốt đến đâu". Phải viết lại phần đóng góp — dù dữ liệu buộc phải đổi.
3. **Ranh giới với domain generalization mờ.** Người phản biện có thể nói *"đây là DG với thống kê test-time"*. Phòng: nhấn mạnh (a) pha 2 hoàn toàn không nhãn, (b) `s` ước lượng trực tuyến, (c) khung CMS/Eq 72-73.
4. **Trần chưa biết.** Cho tới khi chạy cửa chặn Phần V, 12,7 điểm chỉ là **thượng giới lý thuyết**, không phải hứa hẹn.

---

# PHẦN VIII — Lộ trình

| Bước | Nội dung | Thời gian | Cửa chặn |
|---|---|---|---|
| **0** | ⛔ **Oracle trần** (Phần V) | 1h, không GPU | < 0,58 → **BỎ HẲN M0** |
| 1 | Viết `models/tang_nen.py` + `scripts/hoc_tang_nen.py` | ~3h code | `pytest` xanh |
| 2 | Huấn luyện M0 ở pha 1, 1 seed | ~1h | loss tái dựng giảm; acc trên trôi giữ-lại tăng |
| 3 | **U3 = U1 + M0**, 3 seed, stream revisit | ~3h (sau vá `eval_chi_duong_cheo`) | **U3 − U1 > 0** trên O1 |
| 4 | Giao thức giữ-lại (R2): train `{0.25,0.75}` → test `{0.5,1.0}` | ~1h | không tụt quá 30% so với bước 3 |
| 5 | Bench O4 | 10 phút | < 10 MB · < 1,67 ms |

**Đường găng là bước 0.** Mọi thứ sau đó chỉ chạy nếu bước 0 cho phép.

---

# PHẦN IX — Bảng kết quả đích

```
BÀI TOÁN UAV — revisit, pha 2 KHÔNG nhãn, 12 chuyến, 3 seed

                          O1               Δ so U0        MB      ms
  U0  đóng băng           0.596 ± 0.018    —              1.50    0.007
  U1  + tầng nhanh (M1)   0.628 ± 0.007    +0.032 ***     1.51    0.054
  U2  + ngân hàng  (M2)   0.628 ± 0.007    +0.032  (M2 = 0, BỎ)
  U3  + tầng nền   (M0)   _____            _____   ⭐     3.90    0.104
  ─────────────────────────────────────────────────────────────────────
  trần oracle (Phần V)    _____            _____          —       —
  Titans / HOPE           —                —            102.68    2.178   ❌ O4
```

Hai ô trống là toàn bộ phần thực nghiệm còn lại.

---

# PHẦN X — Ba luận điểm sau khi có M0

**① Hiện thực hoá NL bằng gradient không sống được ở biên.**
Cài đủ ba đóng góp. Vượt ngân sách bộ nhớ **10,3×**. Cổng bão hoà 8/13 run trước vá; sau vá cổng sống, state tích luỹ 62→292, nhưng bộ đọc oracle vẫn **thua ViT đóng băng + NCM** (0.6874 < 0.6933).

**② Cùng cấu trúc bằng công thức đóng + một tầng nền học được thì sống được.**
CMS bốn tầng, gồm Eq 72/73 — cơ chế mà **bản gradient cũng bỏ trống**. **3,9 MB · 0,10 ms**: rẻ hơn **26×** bộ nhớ, **21×** độ trễ.

**③ Hai kết quả âm có cơ chế, không phải null trống.**
- *Ghi nhớ điều kiện không đáng giá khi thích nghi rẻ:* λ=0,99 → hội tụ 4 batch; snapshot lưu trữ mờ hơn ước lượng tươi → **−0,0006 ± 0,0010**, ứng viên nạp-sớm còn **hại 1,2 điểm**.
- *Căn chỉnh affine cố định chỉ lấy lại 20% thiệt hại do trôi* (3,2/15,9) — **và đó là lý do M0 tồn tại.**

---

*Bước tiếp theo duy nhất: chạy cửa chặn Phần V. Một giờ, không GPU, quyết định toàn bộ phần còn lại.*
