# Đã vá xong — báo cáo thay đổi & việc cần chạy trên Mac

Ngày: 2026-08-02 · Vá theo `KE_HOACH_VA_NL_2026-08-02.md`

---

## ⚠️ TASK 0 đã LẬT NGƯỢC một phần chẩn đoán — hướng vá đã đổi

Kế hoạch có sẵn hai nhánh cho TASK 4, tuỳ kết quả TASK 0. **Kết quả rơi vào nhánh thứ hai.**

`scripts/trace_gates.py` chạy trên `result_test` cho:

```
TROI-DAN     9 run
LANH-MANH    4 run
BAO-HOA-TU-DAU   0 run     <-- KHÔNG có run nào
```

**α KHÔNG bão hoà từ đầu.** Nó khởi đầu hoàn toàn lành mạnh rồi **trôi đơn điệu ra biên
trong 2–4 task**:

| Run | task 0 | task 1 | task 2 | task 3 | … | task 8 |
|---|---:|---:|---:|---:|---|---:|
| `sdc_s0` | 0.4475 | 0.8616 | 0.9975 | 0.9999 | … | 1.0000 |
| `sdc_s1` | 0.5284 | 0.8842 | 0.9892 | 1.0000 | … | 1.0000 |
| `sdc_s2` | 0.6799 | 0.9387 | 0.9992 | 1.0000 | … | 1.0000 |
| `latreplay_s2` | 0.6799 | 0.9505 | 0.9990 | 0.9997 | … | 1.0000 |
| `m3_s0` | 0.8867 | 1.0000 | 1.0000 | 1.0000 | … | 1.0000 |
| **`m3_s1`** | **0.0962** | **0.0000** | 0.0000 | 0.0000 | … | **0.0000** |

Logit thô tăng gần tuyến tính theo task (`m3_s0`: 5.8 → 18.5 → 25.2 → 33.5 → … → 55.6).

### Nguyên nhân thật — và nó nghiêm trọng hơn "thang đo feature"

Gradient descent phát hiện **quên nhiều hơn làm giảm loss của task HIỆN TẠI** (bớt nhiễu từ
lớp cũ), nên liên tục đẩy α → 1. **Không có gì đẩy ngược lại.** Qua 9 task × 3 epoch, nó
tích luỹ đến bão hoà.

Đây **chính xác là pathology đã gặp ở Mục 5 báo cáo trước** — current-task validation chọn
`gamma=1.0` vì nó tốt cho lớp mới. Cùng một bệnh, giờ lộ ra ở tầng sâu hơn: **mục tiêu huấn
luyện đang thưởng cho việc quên.**

**Hệ quả cho bản vá:** `pre_norm` chỉ sửa thang đo *lúc khởi tạo* — nó **không chặn được đà
trôi do gradient**. Nên thuốc chính phải đổi.

`m3_s1` là ngoại lệ đáng chú ý: trôi về **0** thay vì 1, và `norm` đã là **18.222 ngay ở
task 0** rồi compound lên 1,75 triệu. Run này hỏng từ task đầu, không phải tích luỹ dần.

---

## Thuốc chính mới: `gate_bound` (tanh-bound logit)

```python
logit_bounded = tanh(logit / L) * L
```

- Độ dốc tại 0 **đúng bằng 1** → gần như identity ở vùng giữa, không làm méo hành vi lành mạnh
- Bão hoà **mượt** về ±L → cổng không bao giờ thoát ra biên
- **Gradient không bao giờ chết** → cổng vẫn phụ thuộc dữ liệu (khác hẳn clamp cứng)
- Cài bằng **forward-hook bọc ngoài**, không fork thư viện — cùng triết lý `self_ref_memory.py`

Hiệu quả kiểm bằng số:

| logit thực tế trong log | α không chặn | α **có chặn** | η không chặn | η **có chặn** |
|---:|---:|---:|---:|---:|
| 0.23 | 0.5572 | 0.5571 | 5.57e-03 | 5.57e-03 |
| 5.77 | 0.9969 | **0.9443** | 9.97e-03 | **8.98e-03** |
| 18.40 | 1.0000 | **0.9500** | 1.00e-02 | **9.00e-03** |
| 55.62 | 1.0000 | **0.9500** | 1.00e-02 | **9.00e-03** |
| −5.96 | 0.0026 | **0.0550** | 2.57e-05 | **1.02e-03** |
| −9.20 | 0.0001 | **0.0505** | 1.01e-06 | **1.00e-03** |

Hàng đầu (logit lành mạnh 0.23) gần như **không đổi** — đúng ý đồ: chỉ chặn biên, không đụng vùng giữa.

---

## Các file đã sửa

| File | Task | Thay đổi |
|---|---|---|
| `scripts/trace_gates.py` | 0 | **MỚI** — trích quỹ đạo η/α theo từng task, tự phân loại TROI-DAN / BAO-HOA-TU-DAU / LANH-MANH |
| `src/uavcl/models/memory.py` | 1,2,4 | Sửa ngữ nghĩa α · thêm `_eta_real_sum` + `_detect_eta_max_lr()` · thêm `_install_gate_bounds()` + `gate_bound_report()` · `eta_alpha_stats()` trả **3** giá trị |
| `src/uavcl/models/titans_head.py` | 4,5 | Truyền `gate_bound` xuống · thêm `pre_norm` (mặc định tắt) · `_INIT_BIAS_KEYS` cho TASK 5 · cập nhật docstring `eta_alpha_stats` |
| `src/uavcl/methods.py` | 2,3 | Log `eta_raw`/`eta_real`/`alpha`/`keep` **và giữ nguyên văn dòng cũ** · cảnh báo ⚠️ khi cổng bão hoà |
| `scripts/compare_all.py` | 1,2 | Đọc được **cả hai** định dạng log · tự quy đổi η thật cho log cũ · cột `keep` · cờ ⚠️SAT · sửa chú thích ngược |
| `configs/..._m3_gates.yaml` | 4,5 | **MỚI** — improved + `gate_bound`/`pre_norm`/`init_*`, **không** CMS |
| `configs/..._m3_cms.yaml` | 6 | **MỚI** — như trên + khối `cms.enabled` |
| `tests/test_gate_bound.py` | 4,5 | **MỚI** — 16 test |

### Bất biến ngược được giữ nguyên

- `gate_bound`, `pre_norm`, `init_*_bias` **đều mặc định TẮT** → không config nào cũ bị đổi hành vi
- Dòng log định dạng **cũ vẫn được in song song** → script cũ vẫn đọc được
- `compare_all.py` đọc được cả log cũ lẫn mới

### Thay đổi duy nhất KHÔNG tương thích ngược

`eta_alpha_stats()` trả **3** giá trị thay vì 2. Đã cập nhật cả 2 nơi gọi
(`methods.py`, `titans_head.py`). Nếu có code ngoài repo gọi hàm này thì phải sửa theo.

---

## Chạy trên Mac — theo thứ tự

Sandbox không có `torch` nên mình chỉ chạy được `py_compile` (đã sạch cả 6 file).
**`pytest` bắt buộc phải chạy trên máy bạn.**

### Bước 1 — pytest (CHẶN)

```bash
cd ~/Desktop/Raybanmeta/uav-continual-learning && source .venv/bin/activate && pytest -q
```

Đỏ → dừng, gửi mình lỗi. Ba file dễ vỡ nhất: `test_g2_titans.py`,
`test_self_modifying_memory.py`, `test_g3_cms.py` (do `eta_alpha_stats` đổi chữ ký).

### Bước 2 — Smoke đọc cổng (CỬA CHẶN, bỏ qua accuracy)

```bash
.venv/bin/python scripts/run_g1.py \
  --config configs/g2_titans_resisc45_selfmod_m3_gates.yaml \
  --set data.num_tasks=3 train.epochs_per_task=1 log.dir=./artifacts_smoke_gates \
  2>&1 | tee run_smoke_gates.log

python3 scripts/trace_gates.py .
```

Trong log phải thấy dòng `[titans] gate_bound BẬT — α∈[0.050,0.950]  η∈[1.00e-03,9.00e-03]`.

**Tiêu chí ĐẠT:**

| Chỉ số | Ngưỡng |
|---|---|
| α mỗi task | trong `[0.05, 0.95]` |
| η thật mỗi task | trong `[1e-3, 9e-3]` |
| α biến thiên giữa các task | `max − min ≥ 0.05` |
| `norm(state)` | `< 1e4` |
| `trace_gates.py` phân loại | **`LANH-MANH`**, không phải `DUNG-YEN` |

Tiêu chí 3 và 5 quan trọng nhất: **cổng nằm trong khoảng nhưng đứng yên vẫn là hỏng.**
Eq 76 đòi η/α phải *phụ thuộc dữ liệu*. Nếu ra `DUNG-YEN` thì `gate_bound` đang chặn quá
chặt — nới `alpha_logit_limit` lên 4.0 rồi chạy lại.

### Bước 3 — So 1-biến, 3 seed (chỉ sau khi bước 2 ĐẠT)

```bash
for s in 0 1 2; do
  .venv/bin/python scripts/run_g1.py --config configs/g2_titans_resisc45_selfmod_m3_gates.yaml \
    --set seed=$s train.eval_ncm_head=true train.checkpoint=true log.dir=./artifacts_gates_s$s \
    > run_gates_s$s.log 2>&1
done
```

Đối chứng là các run `improved` cũ (cùng seed, khác đúng khối `gate_bound`).
Mốc: Titans+NCM online cũ `0.6190` · Frozen NCM `0.7114` · **SLDA `0.8263`**.

### Bước 4 — CMS (sau bước 3)

```bash
.venv/bin/python scripts/run_g1.py --config configs/g2_titans_resisc45_selfmod_m3_cms.yaml \
  --set seed=0 log.dir=./artifacts_cms_s0 > run_cms_s0.log 2>&1
```

Xác minh bắt buộc trong log:

1. `[cms] M3 frequency unit=global_step | per-tier f=[...]`
2. `tier_report` in bảng phân tier
3. `‖Δw‖` per-tier: **tier chậm ≈ 0, tier nhanh lớn** — ngược lại thì **DỪNG**, mapping tier sai

### Bước 5 — Thí nghiệm quyết định

Chỉ sau khi bước 3 xong: **SLDA + `memory.enabled=true`**, 3 seed. Giờ mới kết luận được,
vì cổng đã sống — trước đó không phân biệt được "Titans vô dụng" với "Titans tê liệt".

---

## Một điều cần nói thẳng về kỳ vọng

`gate_bound` sửa **triệu chứng** (cổng chạm biên), không sửa **nguyên nhân** (mục tiêu huấn
luyện thưởng cho việc quên). Nó giữ cổng trong vùng hoạt động để Eq 76 còn ý nghĩa, nhưng
gradient vẫn sẽ ép α về sát trần 0.95.

Nếu bước 3 cho thấy accuracy cải thiện ít, **đó không phải thất bại của bản vá** — đó là bằng
chứng cho một kết luận sâu hơn: *trong continual learning không replay, tối ưu loss của task
hiện tại tự nó tạo áp lực chọn quên; cần một số hạng regularization đối kháng, không chỉ chặn biên.*

Kết luận đó có giá trị nghiên cứu độc lập, và giờ bạn có **dữ liệu quỹ đạo 13 run** để chứng minh.
